const arr = [1, [2, [3, 4]]];
const flatArray = arr.flat(2); // (2) depth of array which include them by default
console.log(flatArray); // Output: [1, 2, 3, 4]

//The flat() method creates a new array with all sub-array elements concatenated to the specified depth

// ------------------------------------------------------------------------------------------

