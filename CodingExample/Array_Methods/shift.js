const arr = [1, 2, 3, 4, 5];
const shiftedElement = arr.shift();
console.log(arr); // Output: [2, 3, 4, 5]
console.log(shiftedElement); // Output: 1

//the shift() method removes and returns the first element from the array (like pop)